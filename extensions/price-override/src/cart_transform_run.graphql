query run($cart: Input!) {
  cart {
    lines {
      id
      quantity
      merchandise {
        ... on ProductVariant {
          id
        }
      }
      attribute(key: "Custom_Price") {
        key
        value
      }
    }
  }
}